---
paths:
  /ping:
    x-exegesis-controller: ping
    get:
      tags:
        - "Health Check"
      operationId: ping
      summary: " - Health Check"
      description: |
        Health Check
      responses:
        200:
          description: "OK"
