---
paths:
  /ping:
    x-exegesis-controller: ping
    get:
      operationId: ping
      summary: " - Health Check"
      description: |
        Health Check
      responses:
        200:
          description: "OK"
